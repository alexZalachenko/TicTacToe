
namespace Util
{
    public enum Players
    {
        Human,
        AI
    };

    public enum GameResults
    {
        Tie,
        Win,
        Lose,
        Unfinished
    };

    // Do not change this enum elements order
    public enum Tiles
    {
        Cross,
        Circle,
        Empty
    };

    public enum Algorithms
    {
        Minmax,
        DecisionTree
    }
}
